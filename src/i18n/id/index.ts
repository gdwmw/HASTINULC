import { idButton, idInput } from "./components";
import { idAuthentication, idHome } from "./layouts";

export const id = {
  // ----------------------------
  authentication: idAuthentication,
  button: idButton,
  // ----------------------------
  home: idHome,
  input: idInput,
};
