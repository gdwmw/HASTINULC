import { enButton, enInput } from "./components";
import { enAuthentication, enHome } from "./layouts";

export const en = {
  // ----------------------------
  authentication: enAuthentication,
  button: enButton,
  // ----------------------------
  home: enHome,
  input: enInput,
};
